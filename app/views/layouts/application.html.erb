<!DOCTYPE html>
<html lang="en">
<head>
  <title>Rails General Store</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">

  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
  <style>

  </style>
</head>
<body>

<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>                        
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Rails General Store</a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
      <% if current_user %>
        <li class="active"><a href="<%= distributor_path(current_user) %>"><%="#{current_user.user_name.capitalize}'s Profile"%></a></li>
        <li><a href="<%= new_store_path %>">Create Store</a></li>
        <li><a href="<%= new_item_path %>">Create New Item</a></li>
        <li><a href="<%= items_path %>">All Items</a></li>
        <li><a href="<%= stores_path %>">All Stores</a></li>
        <% end %>

      </ul>

      <ul class="nav navbar-nav navbar-right">
      <% if !current_user %>
        <li><a href="<%= login_path %>"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
        <li><a href="<%= new_distributor_path %>"><span class="glyphicon glyphicon-log-in"></span> Signup</a></li>
        <% else %>
        <li><a href="<%= signout_path %>"><span class="glyphicon glyphicon-log-in"></span> Logout</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
  
<div class="container-fluid text-center">    

  <div class="row content">
    <div class="col-sm-2 sidenav">
    </div>
    <div class="col-sm-8 text-left"> 

</div>

      <%= yield %>
      
    </div>
    <div class="col-sm-2 sidenav">
    <% if flash[:notice] %>
        <div class="notice"><%= flash[:notice] %></div>
      <% end %>
    </div>
  </div>
</div>

<footer class="container-fluid text-center">
  <small>&copy; 2019 <strong>GenStore</strong></small>
</footer>

</body>
</html>